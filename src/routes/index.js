import 'dotenv/config';

/**
 * @description realiza a validação do token
 * @param {*} token informadp
 * @returns boolean
 * @TODO fazer lógica dessa função
 */
const validarToken = (token) => {
  // retornar true por enquanto
  if (token) return true;
};

export default validarToken;
